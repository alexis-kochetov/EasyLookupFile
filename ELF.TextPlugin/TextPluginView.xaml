<UserControl x:Class="ELF.TextPlugin.TextPluginView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" x:Name="view" DataContext="{Binding ElementName=view,Path=TextParameters}" mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="400" Background="{DynamicResource WindowBackgroundBrush}">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition Width="*" />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<Label Grid.Column="0" Content="Substring:" Margin="5" HorizontalAlignment="Right" />
		<TextBox Grid.Column="1" Text="{Binding Path=Substring, Mode=TwoWay}" Margin="5" />
	</Grid>
</UserControl>